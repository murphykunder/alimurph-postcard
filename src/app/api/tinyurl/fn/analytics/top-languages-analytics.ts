/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TopLanguagesAnalyticsResponse } from '../../models/top-languages-analytics-response';

export interface TopLanguagesAnalytics$Params {

/**
 * Datetime, indicating start datetime of analytics report.
 */
  from: string;

/**
 * Datetime, indicating end datetime of analytics report. Will default to current time.
 */
  to?: string;

/**
 * TinyURL to get analytics for
 */
  alias?: string;

/**
 * Tag to get analytics for
 */
  tag?: string;
}

export function topLanguagesAnalytics(http: HttpClient, rootUrl: string, params: TopLanguagesAnalytics$Params, context?: HttpContext): Observable<StrictHttpResponse<TopLanguagesAnalyticsResponse>> {
  const rb = new RequestBuilder(rootUrl, topLanguagesAnalytics.PATH, 'get');
  if (params) {
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
    rb.query('alias', params.alias, {});
    rb.query('tag', params.tag, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TopLanguagesAnalyticsResponse>;
    })
  );
}

topLanguagesAnalytics.PATH = '/analytics/languages/top';
